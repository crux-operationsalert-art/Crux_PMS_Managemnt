-- The screen itself, rebuilt on the functions 155 and 156 added.
--
-- What it could do before: show coverage, rename a place, move a place, align
-- a city, teach a spelling. What the owner could not do, and asked for: add or
-- remove a client at a place, edit a branch, seat a Branch or Zonal Manager,
-- assign a whole place in one go, or price anything. Those were four other
-- screens, or nowhere.
--
-- It is a master and a detail now rather than one long scroll, because the
-- scroll could not carry branches. The tree on the left is the operating
-- grouping, with a branch count against every zone and location and a search
-- that also matches the cities underneath. One place opens on the right, in
-- four tabs:
--
--   Clients & handlers  tickboxes, "assign this whole place", "assign the
--                       ticked", the handlers with product and start date,
--                       the rate in force, and Add / Remove for the client.
--   Who runs it         two tables, because the owner said they are two
--                       things. Who runs the place today is a dated role;
--                       who is seated in a chair here is the job.
--   Branches            a search with add and edit, because Mumbai has 1,665.
--   Rates               set one, and the version in force is end-dated the
--                       day before it starts.
--
-- 157a  the splice. Bounded by two markers, each asserted present exactly
--       once and in order before a byte is written, and nine markers asserted
--       present and five asserted gone afterwards. Source kept at
--       build/app/screen-places.js, byte-identical to what the page now
--       holds (md5 065df9251c3f60fafedcd8bc71dc2b88, 37,025 bytes).
-- 157b  the splice was bounded by the first FUNCTION of the old screen, not
--       its first byte, so the old banner comment and one dead module-level
--       variable survived above the cut. covOpts was referenced exactly once
--       in the whole page: its own declaration.
-- 157c  same cause, second casualty -- the 150-era banner for this screen,
--       describing a scope two migrations out of date, sitting directly on
--       top of the banner that replaced it.
--
-- The CSS this screen needs was added and none was removed: .opzone and
-- .oploc looked like leftovers from 150 but turned out to belong to the org
-- tree on another screen. Checked before deleting, not after.
--
-- Verified, not assumed: every ops/places route probed over pg_net after the
-- deploy, 10 of 10 at 200, probe session revoked afterwards; the screen
-- rendered in Chromium against real data shapes with zero console errors and
-- every tab exercised; the four tab renders reviewed by eye, which is how the
-- cramped client column and the uppercased "DELHI IN DELHI" header were
-- caught -- the place name is the heading now and "Location - 208 branches"
-- is a quiet line under it. build/app/screen-places.png is the render that
-- passed.
